This repository contains a data platform created as an example and proof of concept.

The data layout is a datalake style medallion architecture with 3 layers, bronze (raw data), silver(cleaned + standardized) and gold(aggregated data set following data governance best practices and ready for powerBI or other reporting products)